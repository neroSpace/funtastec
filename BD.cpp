#include <iostream>
#include <cmath>
#include <cstdio>
using namespace std;

int main(){
		int N, arr[105];
		while(scanf("%d", &arr[N]) != EOF){
			N++;
		}
		
		while(N--){
			cout << arr[N] << endl;
		}
}
